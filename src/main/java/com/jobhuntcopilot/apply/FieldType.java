package com.jobhuntcopilot.apply;

/** The kind of input control a scanned form field renders as. */
public enum FieldType {
    TEXT,
    EMAIL,
    TEL,
    TEXTAREA,
    SELECT,
    RADIO_GROUP,
    CHECKBOX,
    FILE
}
