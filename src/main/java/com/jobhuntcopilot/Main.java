package com.jobhuntcopilot;

/**
 * Entry point for Job Hunt Copilot.
 *
 * Phase 0 just proves the Maven skeleton builds and runs. Each later phase
 * wires more of the real app in behind this: config/data layer (Phase 1),
 * job fetching (Phase 2), scoring (Phase 3), and the JavaFX GUI (Phase 4+),
 * which will eventually replace this console entry point as the way the
 * app is launched.
 */
public class Main {

    public static void main(String[] args) {
        System.out.println("Job Hunt Copilot — Phase 0 skeleton is running.");
    }
}
